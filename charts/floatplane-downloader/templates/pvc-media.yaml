{{- if not .Values.floatplane.persistence.media.useExistingPvc }}
# templates/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Chart.Name }}-media
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "floatplane-downloader.labels" . | nindent 4 }}
{{- with .Values.floatplane.persistence.media.spec }}
spec:
{{ toYaml . | nindent 2 }}
{{- end }}
{{- end }}
